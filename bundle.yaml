apiVersion: "1"
version: "1"
id: "cloudbees-ci-workshop-REPLACE_CONTROLLER_NAME"
description: "CloudBees CI configuration bundle for the CloudBees CI Workshop"
availabilityPattern: "REPLACE_AVAILABILITY_PATTERN"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
catalog:
  - "plugin-catalog.yaml"
